[package]
name = "hive-coordinator"
description = "Hive scheduler library — runs inside hive-server on whichever regional server holds the coordinator lease (ADR-005). Pure matching logic lives here so it is unit-testable without Postgres."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
hive-core = { workspace = true, default-features = false }
serde.workspace = true
chrono.workspace = true
uuid.workspace = true
tracing.workspace = true

[dev-dependencies]
serde_json.workspace = true
