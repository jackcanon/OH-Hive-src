// @ohhive/ui — React components shared by apps/web and apps/desktop (ADR-009 D33).
// Keep this dependency-free beyond React so both Vite and Next can consume the source directly.

export { AboutSection } from "./AboutSection";
export type { AboutInfo } from "./AboutSection";
